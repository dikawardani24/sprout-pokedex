name: feature_home
description: "A new Flutter module project."

version: 1.0.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../app_core/core
  core_ui:
    path: ../../app_core/core_ui
  cupertino_icons:
  flutter_bloc:
  get_it:
  injectable:
  equatable:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  injectable_generator:
  build_runner:

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.feature_home
    iosBundleIdentifier: com.example.featureHome
