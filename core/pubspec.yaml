name: core
description: "Core utilities and shared code for Sprout Pokedex"
version: 1.0.0

environment:
  sdk: ^3.5.3  # ← KEEP THIS!

dependencies:
  flutter:
    sdk: flutter

  # These get their versions from main app via Melos
  equatable:
  flutter_bloc:
  bloc_concurrency:
  stream_transform:
  collection:
  freezed_annotation:
  pokedex:
  injectable:
  cached_network_image:
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed:
  injectable_generator:
  mockito:
  mocktail:
  flutter_lints:
