name: app_preference
description: "A new Flutter module project."
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  shared_preferences:
  get_it:
  injectable:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  build_runner:
  injectable_generator:

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.app_preference
    iosBundleIdentifier: com.example.appPreference
