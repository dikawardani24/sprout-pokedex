name: api
description: "A new Flutter module project."
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  pokedex:
  injectable:
  get_it:
  http_interceptor:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  build_runner:
  injectable_generator:
  mocktail:
  mockito:

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: com.example.api
    iosBundleIdentifier: com.example.api
